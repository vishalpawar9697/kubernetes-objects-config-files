# 1) how to install minicube

wget https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64

# 2) copy downloaded file in location

sudo cp minikube-linux-amd64 /usr/local/bin/minikube

# 3) gave the exicutive perrmition

sudo chmod 755 /usr/local/bin/minikube

# 4) finally check yo have sucssessfuly criate or not

sudo chmod 755 /usr/local/bin/minikube

# step:- install kubectl

# 1) download kubectl following command

curl -LO https://storage.googleapis.com/kubernetes-release/release/`curl -s https://storage.googleapis.com/kubernetes-release/release/stable.txt`/bin/linux/amd64/kubectl

# 2) gave the executive permmition in directory

chmod +x ./kubectl

# 3) then move this files other locaation

sudo mv ./kubectl /usr/local/bin/kubectl

# 4) veryfy installation

kubectl version -o json

# step3:- to start kubctl

minikube start
